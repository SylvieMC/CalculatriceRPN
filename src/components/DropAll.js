import React from 'react';
import '../css/style.css';


const dropAll = (props) => {
    return (
        <button type="button" onClick={props.click}>DROP ALL</button>
    )
};

export default dropAll;
